const config = {
    pg: {
        user: "postgres",
        password: "123",
        host: "localhost",
        port: "5432",
        database: "leao",
        sslmode: "require"
    }
};

module.exports = config;